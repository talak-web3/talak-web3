{
  "name": "example-gasless-tx",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'Dev server requires dependencies'",
    "build": "echo 'App build requires dependencies' && if not exist dist mkdir dist && echo export default {} > dist/main.js",
    "preview": "echo 'Preview requires dependencies'"
  },
  "dependencies": {
    "@talak-web3/client": "workspace:*",
    "@talak-web3/tx": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^5.4.11"
  }
}
