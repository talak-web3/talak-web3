================================================================================
  AUDIT FIX PROMPT — talak-web3-sdk
  Generated: 2026-07-23
  Source: .opencode/audit-reports/REPORT.md
  Severity: ALL (Critical/High/Medium/Low)
================================================================================

You are a fix-agent. Resolve every audit finding below in the talak-web3
pnpm monorepo at C:\talak\talak-web3. Follow each task precisely. Do NOT
modify test files unless the task explicitly says to. Do NOT add comments
unless the fix requires explaining non-obvious logic. Preserve existing
code style: no tabs, double quotes, semicolons, 2-space indentation.

After ALL fixes are applied, run these commands in order and report results:
  1. pnpm run typecheck
  2. pnpm run lint
  3. pnpm run test:unit

If any command fails, fix the regression before proceeding.

================================================================================
SECTION A — HIGH PRIORITY (3 tasks)
================================================================================

--- H1: Fix process.env.X! non-null assertions in CLI template strings ---
File: packages/cli/src/commands/add.ts
Lines: 88, 104

The function generateIntegrationConfig() returns template strings that will
be written to user projects. The ! assertions will crash at runtime if the
env var is missing. Replace with fallback defaults.

CHANGE line 88:
  BEFORE: projectId: process.env.WALLETCONNECT_PROJECT_ID!,
  AFTER:  projectId: process.env.WALLETCONNECT_PROJECT_ID ?? "YOUR_WALLETCONNECT_PROJECT_ID",

CHANGE line 104:
  BEFORE: rpId: process.env.SIWE_DOMAIN!,
  AFTER:  rpId: process.env.SIWE_DOMAIN ?? "localhost",


--- H2: Replace redis?: any with proper typed interface ---
File: packages/core/src/auth-handler.ts
Line: 52-53

STEP 1 — Add a RedisLike interface after the last import statement (around
line 12), before the AuthHandlerOptions type:

  interface RedisLike {
    get(key: string): Promise<string | null>;
    set(key: string, value: string, ...args: unknown[]): Promise<unknown>;
    del(...keys: string[]): Promise<unknown>;
    expire(key: string, seconds: number): Promise<unknown>;
    ping(): Promise<string>;
  }

STEP 2 — Replace lines 52-53:
  BEFORE:
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    redis?: any;
  AFTER:
    redis?: RedisLike;

STEP 3 — Search this file for any usage of `redis.` that accesses a method
not on RedisLike (e.g., .subscribe, .publish, .pipeline, .multi). If found,
add those methods to the RedisLike interface with appropriate signatures.
Run: grep -n "redis\." packages/core/src/auth-handler.ts
to check all usages.

STEP 4 — Remove the eslint-disable comment if it still exists.


--- H3: Remove dead vitest coverage threshold ---
File: vitest.config.ts
Lines: 64-69

DELETE the following 6 lines entirely:

        "packages/middleware/": {
          lines: 90,
          functions: 90,
          branches: 85,
          statements: 90,
        },

There is no packages/middleware/ directory. This threshold is dead config.


================================================================================
SECTION B — MEDIUM PRIORITY (6 tasks)
================================================================================

--- M1: Reduce unsafe `as` assertions in core package ---
File: packages/core/src/index.ts (PRIMARY TARGET)
File: packages/core/src/auth-handler.ts (SECONDARY)

Focus ONLY on these two files. Do not attempt to fix all 98 assertions
across the entire monorepo.

In packages/core/src/index.ts:
  - Find all `as Record<string, unknown>` assertions.
  - For each one, determine if the input type can be narrowed with a type
    guard or explicit property access instead.
  - If the assertion is on a validated Zod input, use z.output<> type instead.
  - For `as unknown as X` double-assertions, add a // SAFETY: comment
    explaining why the cast is necessary.
  - Do NOT change `as const` assertions (those are idiomatic).

In packages/core/src/auth-handler.ts:
  - Find the `as` assertion near line 238 (JSON.parse body).
  - Verify the body is already bounded by MAX_AUTH_BODY_SIZE. If so, add a
    type guard function instead of the assertion.


--- M2: Replace console.log/warn/error with Pino logger in backend ---
Files to fix:
  - apps/hono-backend/src/server.ts
  - apps/hono-backend/src/security/redis-hardening.ts
  - apps/hono-backend/src/security/adaptive-rate-limit.ts
  - apps/hono-backend/src/security/stores.ts
  - apps/hono-backend/src/security/deployment-isolation.ts
  - apps/hono-backend/src/security/cors.ts

The backend already has a Pino logger at apps/hono-backend/src/logger.ts.

For each file:
  1. Add import at top: import { logger } from "../logger.js";
     (adjust relative path per file depth)
  2. Replace console calls using this mapping:
     - console.error("[CRITICAL] ...")  -> logger.fatal({ err }, "...")
     - console.error("[...] ...")       -> logger.error({ err }, "...")
     - console.warn("[...] ...")        -> logger.warn("...")
     - console.log("[BOOTSTRAP] ...")   -> logger.info("...")
     - console.debug(...)               -> logger.debug("...")
  3. EXCEPTION: In server.ts lines 43-44 (startup crash handler), keep
     console.error since the logger may not be initialized yet.
  4. EXCEPTION: In server.ts line 110 ("[BOOTSTRAP] All Redis clusters
     connected: OK"), keep as logger.info.

Pattern for error logging:
  BEFORE: console.error("[CRITICAL] Startup failed:", err)
  AFTER:  logger.fatal({ err }, "Startup failed")

Pattern for warning logging:
  BEFORE: console.warn("[SECURITY] Redis AUTH is disabled")
  AFTER:  logger.warn("Redis AUTH is disabled")

Pattern for info logging:
  BEFORE: console.log("[BOOTSTRAP] Environment validation: PASSED")
  AFTER:  logger.info("Environment validation: PASSED")


--- M3: Fix TypeDoc configuration and generate API docs ---
File: typedoc.json

STEP 1 — Fix the hooks entry point path:
  BEFORE: "packages/hooks/src/index.tsx"
  AFTER:  "packages/hooks/src/index.ts"
The file is .ts, not .tsx.

STEP 2 — Add missing packages to the "entryPoints" array. Add these 9:
  "packages/adapters/src/index.ts",
  "packages/ai/src/index.ts",
  "packages/cli/src/index.ts",
  "packages/dashboard/src/index.ts",
  "packages/identity/src/index.ts",
  "packages/rate-limit/src/index.ts",
  "packages/realtime/src/index.ts",
  "packages/templates/src/index.ts",
  "packages/test-utils/src/index.ts",

STEP 3 — Run: pnpm run docs:api
If typedoc is not in scripts, run: npx typedoc
Verify docs/api/ directory is created.


--- M4: Add JSDoc to @talak-web3/types exports ---
File: packages/types/src/index.ts

Add a one-sentence JSDoc comment to EVERY exported type/interface that
currently lacks one. Priority order:

1. Branded types (add descriptive context):
   /** A 0x-prefixed hex string representing a blockchain address. */
   export type Address = `0x${string}`;

   /** A 0x-prefixed hex string representing arbitrary data or a hash. */
   export type Hex = `0x${string}`;

   /** A chain ID as a numeric identifier (e.g., 1 for Ethereum mainnet). */
   export type ChainId = number;

   /** A Unix timestamp in milliseconds. */
   export type UnixMs = number;

2. Logger:
   /** Structured logger interface with standard log levels. */
   export type Logger = { ... }

3. Store interfaces (NonceStore, RefreshStore, RevocationStore):
   /** Store for single-use SIWE nonces. Implementations must consume nonces on verification. */
   /** Store for refresh sessions with rotation support. */
   /** Store for revoked JWT tokens by jti. */

4. RPC types (IRpc, RpcEndpoint, RpcOptions, RpcCache):
   /** JSON-RPC endpoint with URL and optional chain ID. */
   /** Options for RPC request dispatching including timeout and retries. */
   /** Cache configuration for RPC responses. */
   /** Unified RPC client interface supporting multi-chain routing. */

5. Auth types (SessionPayload, JsonWebKey, JwksResponse):
   /** JWT session payload containing address, chain ID, and expiration. */
   /** JSON Web Key in standard JWK format. */
   /** JWKS endpoint response containing an array of signing keys. */

6. Extension interfaces (IHookRegistry, IMiddlewareChain):
   /** Registry for lifecycle hooks (rpc:before, auth:after, etc.). */
   /** Ordered chain of middleware handlers for request processing. */

7. Config types (TalakWeb3BaseConfig, TalakWeb3Plugin, TalakWeb3Context):
   /** Base configuration shape accepted by talakWeb3(). */
   /** Plugin interface for extending SDK functionality. */
   /** Runtime context passed to middleware and hook handlers. */

8. AI types (AiAgent, ToolDefinition, AgentRunInput, AgentRunOutput):
   /** AI agent definition with model, tools, and instructions. */
   /** Tool that an AI agent can invoke during execution. */
   /** Input payload for an agent execution run. */
   /** Output result from an agent execution run. */

9. Remaining (AnalyticsEvent, AnalyticsSink, Role, Organization, OrgGate):
   /** Analytics event with name, timestamp, and properties. */
   /** Analytics sink for persisting events to external systems. */
   /** User role for organization-level access control. */
   /** Organization entity with members and configuration. */
   /** Organization gate for enforcing membership before access. */

10. Remaining types: PresetName, TalakWeb3Input, TalakWeb3Instance, TalakWeb3EventsMap,
    TalakWeb3Middleware, MiddlewareHandler.
    Add appropriate one-line JSDoc to each.


--- M5: SKIP (orphaned experimental packages are acceptable) ---
No action required. The 6 orphaned packages (utils, dashboard, identity,
realtime, tx, ai) are published for external consumption. Their dead-export
status within the monorepo is by design.


--- M6: Document env mutation pattern in deployment-isolation.ts ---
File: apps/hono-backend/src/security/deployment-isolation.ts

Add a module-level JSDoc comment at the top of the file (after the imports,
before the Environment type export) explaining:

  /**
   * Deployment Isolation Module
   *
   * This module intentionally mutates process.env to configure per-environment
   * settings (log levels, Redis DB numbers, rate limit multipliers, security
   * flags). This pattern is used to propagate deployment-specific configuration
   * to all modules that read process.env without requiring explicit parameter
   * passing.
   *
   * Security note: process.env mutations are global and affect all concurrent
   * requests. The isolateEnvironment() method must be called once at startup
   * before accepting traffic. Concurrent calls to different environments are
   * NOT supported.
   */

No code change needed — documentation only.


================================================================================
SECTION C — LOW PRIORITY (5 tasks)
================================================================================

--- L1: Wire tsconfig references (SKIP) ---
This requires structural changes to 7+ tsconfig.json files and affects the
build pipeline. Skip unless explicitly requested. The root tsconfig.json
already has correct references for tsc --build.


--- L2: Complete CODE_OF_CONDUCT.md ---
File: CODE_OF_CONDUCT.md

The file currently has only "Our Pledge" and "Our Standards" (23 lines).
Append the remaining 4 sections from Contributor Covenant v2.1:

After the current content, add:

## Enforcement Responsibilities

Community leaders are responsible for clarifying and enforcing our standards
of acceptable behavior and will take appropriate and fair corrective action in
response to any behavior that they deem inappropriate, threatening, offensive,
or harmful.

Community leaders have the right and responsibility to remove, edit, or reject
comments, commits, code, wiki edits, issues, and other contributions that are
not aligned to this Code of Conduct, and will communicate reasons for moderation
decisions when appropriate.

## Scope

This Code of Conduct applies within all community spaces, and also applies when
an individual is officially representing the community in public spaces.
Examples of representing our community include using an official e-mail address,
posting via an official social media account, or acting as an appointed
representative at an online or offline event.

## Enforcement

Instances of abusive, harassing, or otherwise unacceptable behavior may be
reported to the project maintainer at [INSERT CONTACT EMAIL]. All complaints
will be reviewed and investigated promptly and fairly.

All community leaders are obligated to respect the privacy and security of the
reporter of any incident.

## Attribution

This Code of Conduct is adapted from the [Contributor Covenant][homepage],
version 2.1, available at
https://www.contributor-covenant.org/version/2/1/code_of_conduct.html.

[homepage]: https://www.contributor-covenant.org


--- L3: (FALSE POSITIVE) console.log in middleware ---
File: packages/talak-web3/src/integrations/nextjs/middleware.ts
Line: 55

The console.log is already gated behind `if (debug)` at line 54. This is
correct behavior — the debug flag controls verbosity. No fix needed.
SKIP this finding.


--- L4: Replace non-null assertions in production source code ---
Fix the following 5 specific locations. Do NOT touch test files.

1. packages/rpc/src/circuit-breaker.ts line 190:
   BEFORE: const excessLatency = latency - this.config.latencyThreshold!;
   AFTER:  const threshold = this.config.latencyThreshold ?? 1000;
           const excessLatency = latency - threshold;

   Line 191:
   BEFORE: failureWeight = Math.min(3, 1 + excessLatency / this.config.latencyThreshold!);
   AFTER:  failureWeight = Math.min(3, 1 + excessLatency / threshold);

2. packages/rpc/src/circuit-breaker.ts line 315:
   BEFORE: const latencyRatio = stats.average / this.config.latencyThreshold!;
   AFTER:  const latencyRatio = stats.average / (this.config.latencyThreshold ?? 1000);

3. packages/rpc/src/index.ts line 308:
   BEFORE: from: lastFailedUrl!,
   Context: lastFailedUrl is set on line 286 from endpoint.url which is
   always a string. The assertion is safe due to control flow. However,
   add a guard for clarity:
   AFTER:  from: lastFailedUrl ?? endpoint.url,

4. packages/core/src/auth-handler.ts line 391:
   BEFORE: const refreshed = await auth.refresh(refreshToken!, context);
   Context: needsRefresh is true only when !!refreshToken is true (line 388).
   The assertion is safe. For clarity, refactor:
   AFTER:  if (!refreshToken) break; // unreachable but satisfies type checker
           const refreshed = await auth.refresh(refreshToken, context);

   OR simply: use a local variable:
     const token = refreshToken;
     if (!token) break;
     const refreshed = await auth.refresh(token, context);

5. packages/core/src/connections.ts line 64:
   BEFORE: return this.redisInstances.get(instanceKey)!;
   AFTER:  const instance = this.redisInstances.get(instanceKey);
           if (!instance) {
             throw new TalakWeb3Error("Redis instance not found after creation", {
               code: "REDIS_INSTANCE_MISSING",
               status: 500,
             });
           }
           return instance;


--- L5: Add inline code example to root README.md ---
File: README.md

Insert a new section after "### Manual Setup" (after line 41, before
"### React integration" on line 43):

  ### Usage Example

  ```ts
  import { talakWeb3, MainnetPreset } from "talak-web3";

  const app = talakWeb3({
    preset: MainnetPreset,
    rpcUrls: ["https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY"],
  });

  // SIWE login
  const nonce = await app.auth.getNonce(address);
  const session = await app.auth.verify({ message, signature });

  // RPC request
  const blockNumber = await app.rpc.request({
    chainId: 1,
    method: "eth_blockNumber",
    params: [],
  });
  ```


================================================================================
SECTION D — VERIFICATION CHECKLIST
================================================================================

After completing all fixes above, verify:

[ ] pnpm run typecheck passes with zero errors
[ ] pnpm run lint passes (or only pre-existing warnings)
[ ] pnpm run test:unit passes
[ ] No new `any` types introduced (grep -r ": any" packages/ apps/ --include="*.ts" | grep -v test | grep -v node_modules | grep -v dist)
[ ] No new `@ts-ignore` or `@ts-expect-error` added
[ ] docs/api/ directory exists (from M3)
[ ] All JSDoc comments have proper formatting (start with /**)

Report a summary table at the end:

| Task | Status | Files Changed | Notes |
|------|--------|---------------|-------|
| H1   | DONE   |               |       |
| H2   | DONE   |               |       |
| H3   | DONE   |               |       |
| M1   | DONE   |               |       |
| M2   | DONE   |               |       |
| M3   | DONE   |               |       |
| M4   | DONE   |               |       |
| M5   | SKIP   |               |       |
| M6   | DONE   |               |       |
| L1   | SKIP   |               |       |
| L2   | DONE   |               |       |
| L3   | SKIP   |               |       |
| L4   | DONE   |               |       |
| L5   | DONE   |               |       |

================================================================================
END OF FIX PROMPT
================================================================================
