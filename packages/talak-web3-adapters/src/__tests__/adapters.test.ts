import { describe, it, expect, beforeEach } from 'vitest';
// Integration tests for Better-Web3 core + adapters.
describe('Adapters', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
